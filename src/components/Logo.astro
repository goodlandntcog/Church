---
interface Props {
  size?: number;
  class?: string;
}
const { size = 52, class: className = '' } = Astro.props;
---

<!-- Placeholder mark — swap for the real church logo (e.g. public/logo.png) when available. -->
<div
  class={`rounded-full bg-navy grid place-items-center shrink-0 ${className}`}
  style={`width:${size}px;height:${size}px;`}
>
  <svg width={size * 0.5} height={size * 0.5} viewBox="0 0 24 24" fill="none" stroke="#ffd200" stroke-width="2" stroke-linecap="round">
    <path d="M12 3v18M6 9h12" />
  </svg>
</div>
